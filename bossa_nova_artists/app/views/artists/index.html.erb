<h3>Bossa Nova Artists</h3>
  <ul>
    <%@artists.each do |artist|%>
    <li><%=link_to artist.name, artist_path(artist.id)%></li>
    <%end%>
  </ul>
  <p><%=link_to "Add an Artist", new_artist_path%></p>
  